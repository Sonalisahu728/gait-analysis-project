# ---- Default: H2 in-memory DB, zero setup, resets on restart ----
spring.datasource.url=jdbc:h2:mem:gaitdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

server.port=8080

# ---- Switch to Postgres when you're ready (uncomment these, comment out the H2 block above) ----
# spring.datasource.url=jdbc:postgresql://localhost:5432/gaitdb
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.datasource.username=postgres
# spring.datasource.password=yourpassword
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
